"use client";
import { motion } from "framer-motion";
import { SectionHeader } from "@/components/ui/section-header";
import { lifeSteps } from "./career-data";
export default function LifeInvolead(){return <section className="bg-[#171717] py-20 text-white"><div className="container mx-auto"><SectionHeader eyebrow="Life at InvoLead" title="Learn, Build, and Grow With Teams That Care About Impact" description="Work on meaningful problems, keep learning, and grow with people who care about the quality and impact of what they build." tone="light" maxWidth="4xl"/><div className="relative mt-14 grid gap-4 md:grid-cols-4 before:absolute before:left-[10%] before:right-[10%] before:top-5 before:hidden before:h-px before:bg-secondary/70 md:before:block">{lifeSteps.map((step,i)=><motion.article key={step.number} initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.1}} className="relative rounded-2xl bg-[#eef8fa] p-6 text-slate-900"><span className="relative z-10 grid size-9 place-items-center rounded-full bg-secondary text-sm font-semibold text-white">{step.number}</span><h3 className="mt-5 font-semibold">{step.title}</h3><p className="mt-3 text-sm leading-6 text-slate-600">{step.description}</p></motion.article>)}</div></div></section>}
